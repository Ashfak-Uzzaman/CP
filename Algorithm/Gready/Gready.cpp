#include <bits/stdc++.h>
using namespace std;

int main()
{
    /*int s = 55;
    int a[3] = {20, 5, 10};
    sort(a, a + 3, greater<int>());
    int i = 0, c = 0;
    while (s != 0)
    {
        if (s / a[i] == 0)
        {
            i++;
        }
        int x = s / a[i];
        s -= x * a[i];
        c += x;
    }
    cout << c;*/

/*Bin Packing Problem*/

    


    return 0;
}
